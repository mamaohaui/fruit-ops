# CLAUDE.md — 中国水果产区数据大屏

## 项目概述

中国水果产区数据大屏 —— 以ECharts交互式地图为核心，展示全国水果产区的数据可视化系统。

## 技术栈

| 层 | 技术 |
|---|---|
| 后端 API | Python 3 + Flask (`server.py`) |
| 数据存储 | openpyxl (Excel `.xlsx` 作为数据库) |
| 前端大屏 | ECharts + 原生 HTML/JS |
| 数据生成/ETL | Python 脚本（openpyxl） |

## 项目结构

```
C:\fruit-ops\
├── CLAUDE.md                       # 本文件
├── reasonix.toml                   # AI 权限配置
├── server.py                       # Flask REST API（CRUD on Excel）
├── fruit_data.xlsx                 # 数据存储（4个Sheet）
├── 中国水果产区数据大屏.html    # ECharts 交互式地图
├── generate_fruit_data.py          # 从权威来源生成水果数据
├── update_fruit_data.py            # 数据质量修复 + 新增50种水果
├── enrich_fruit_data.py            # 增加西瓜、菠萝、樱桃等品种
└── 存档/                           # 对话记录、导出文档等长期产出物
```

## 架构

```
Flask server.py  ◄─── HTTP REST API ───► 中国水果产区数据大屏.html
      │
      ▼
openpyxl ──► fruit_data.xlsx（4个Sheet）
      │
      ▼ (重新生成嵌入式 JSON)
  中国水果产区数据大屏.html (ECharts 地图)
```

## AI 行为准则

### 0. 存档目录隔离

`存档/` 目录存放对话记录、导出文档等长期产出物。**AI 禁止主动读取、搜索或引用该目录下的任何文件**——包括 `Glob`、`Grep`、`Read` 等所有文件操作。只有当用户**明确指定**某个存档文件时（如"读一下 存档/xxx.md"），才允许访问。

### 1. 先思考再编码
不假设。不隐藏疑惑。摆出权衡。
实施前明确假设，如有多种解释则提出，存在更简单方案时指出来。

### 2. 简单至上
最小化代码解决问题。不添加未要求特性，不为一次性代码做抽象。

### 3. 精确改动
只动必须要动的。不"改进"相邻代码，匹配现有风格，清理自己产生的孤儿。

### 4. 目标驱动执行
定义成功标准，循环直到验证。多步骤任务先写计划，每步附带验证方式。

### 5. 任务收尾：清理临时产物
每个独立任务完成后，主动检查本次会话是否产生了临时文件、中间数据或调试脚本。
判断标准：
- **删除**：仅服务于本次任务的中间产物（一次性脚本、调试输出、测试数据、下载的临时文件）。
- **合并/归档**：产出的内容有长期价值但与现有文件重叠 → 合并到正式文件后删除临时副本。
- **保留**：本身就是交付物（如新的管理表、方案文档），按项目结构放到正确目录。

操作步骤：
1. 任务完成时列出本次新增/修改的全部文件。
2. 逐文件判定：删、并、留。
3. 执行清理，确保 `git status`（如使用 git）干净且无遗留的临时文件。
4. 如果判定为保留但位置不妥，移到项目结构约定的目录。
