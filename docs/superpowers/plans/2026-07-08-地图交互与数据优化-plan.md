# 地图交互与数据优化 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现省份点击平滑 zoom、高德 API 自动获取坐标、生长期绿色空心圆三个优化。

**Architecture:** 纯前端改动（HTML 中 JS + HTML/CSS）和高德 API 后端集成（server.py）。前端重写地图交互逻辑，移除子地图切换机制，改用 ECharts 原生 center/zoom 动画。后端新增 geocode() 函数，在创建/更新/同步产区时对坐标为 0 的行自动补全。

**Tech Stack:** ECharts (中国地图), Python 3 + Flask + openpyxl, 高德 Web API v3/geocode/geo, 标准库 urllib + hashlib

## Global Constraints

- 始终使用中文回复和注释
- 修改最小化，不动不相关的代码
- 匹配现有代码风格（var 声明、function 表达式、单引号）
- 高德 Key: `d60264473fe914fa0cf21d1a22a4e206`，安全密钥: `ae7d546c1e8f25d8365b2c601fd3c96a`

---

### Task 1: 后端 — 高德 API 地理编码模块

**Files:**
- Modify: `server.py` (顶部新增 import 和配置 + 新增 geocode 函数)

**Interfaces:**
- Produces: `geocode(province, city, district, town)` → `(lng: float, lat: float)`

- [ ] **Step 1: 添加 import 和配置常量**

在 server.py 顶部现有 import 之后，添加：

```python
import hashlib
import urllib.request
import urllib.parse
import json as json_lib

# 高德地图 Web API 配置
AMAP_KEY = "d60264473fe914fa0cf21d1a22a4e206"
AMAP_SECRET = "ae7d546c1e8f25d8365b2c601fd3c96a"
```

- [ ] **Step 2: 添加 geocode() 函数**

在 `_auto_fill_lunar()` 函数之后、路由之前，添加：

```python
def geocode(province="", city="", district="", town=""):
    """调用高德地理编码 API，返回 (lng, lat)，失败返回 (0, 0)。"""
    parts = [p for p in [province, city, district, town] if p and str(p).strip()]
    address = "".join(parts)
    if not address.strip():
        return 0, 0
    try:
        params = f"address={urllib.parse.quote(address)}&output=JSON"
        sig_raw = f"/v3/geocode/geo?{params}&key={AMAP_KEY}{AMAP_SECRET}"
        sig = hashlib.md5(sig_raw.encode()).hexdigest()
        url = f"https://restapi.amap.com/v3/geocode/geo?{params}&key={AMAP_KEY}&sig={sig}"
        req = urllib.request.Request(url)
        with urllib.request.urlopen(req, timeout=5) as resp:
            data = json_lib.loads(resp.read().decode("utf-8"))
        if data.get("status") == "1" and data.get("geocodes"):
            loc = data["geocodes"][0].get("location", "0,0")
            parts = loc.split(",")
            if len(parts) == 2:
                return float(parts[0]), float(parts[1])
    except Exception as e:
        print(f"[geocode] 查询失败: {address}, 错误: {e}")
    return 0, 0
```

- [ ] **Step 3: 验证 geocode 函数可用**

```bash
cd c:\fruit-ops && python -c "from server import geocode; print(geocode('四川省', '成都市', '龙泉驿区'))"
```
预期: 返回类似 `(104.27, 30.56)` 的非零坐标。

- [ ] **Step 4: Commit**

```bash
git add server.py
git commit -m "feat: 添加高德 API 地理编码模块 geocode()"
```

---

### Task 2: 后端 — 集成 geocode 到三个端点

**Files:**
- Modify: `server.py` (create_fruit, update_fruit, sync_all)

**Interfaces:**
- Consumes: `geocode(province, city, district, town)` from Task 1

- [ ] **Step 1: 集成到 POST /api/fruits (create_fruit)**

在 `create_fruit()` 函数中，写产区行（约 306-308 行）之前，添加坐标补全逻辑。

找到这一段（约 line 299-312）：
```python
    # Also add a region mapping row for this fruit
    new_region_row = ws_r.max_row + 1
    ws_r.cell(new_region_row, R_ADCODE).value = str(body.get('adcode', '000000000')).strip() or '000000000'
    ws_r.cell(new_region_row, R_PROV).value = body.get('province', '')
    ws_r.cell(new_region_row, R_CITY).value = body.get('city', '')
    ws_r.cell(new_region_row, R_DIST).value = body.get('district', '')
    ws_r.cell(new_region_row, R_TOWN).value = body.get('town', '')
    try: ws_r.cell(new_region_row, R_LNG).value = float(body.get('lng', 0))
    except: ws_r.cell(new_region_row, R_LNG).value = 0
    try: ws_r.cell(new_region_row, R_LAT).value = float(body.get('lat', 0))
    except: ws_r.cell(new_region_row, R_LAT).value = 0
```

替换为：
```python
    # Also add a region mapping row for this fruit
    new_region_row = ws_r.max_row + 1
    ws_r.cell(new_region_row, R_ADCODE).value = str(body.get('adcode', '000000000')).strip() or '000000000'
    ws_r.cell(new_region_row, R_PROV).value = body.get('province', '')
    ws_r.cell(new_region_row, R_CITY).value = body.get('city', '')
    ws_r.cell(new_region_row, R_DIST).value = body.get('district', '')
    ws_r.cell(new_region_row, R_TOWN).value = body.get('town', '')
    try: lng = float(body.get('lng', 0))
    except: lng = 0
    try: lat = float(body.get('lat', 0))
    except: lat = 0
    # 若坐标为 0，调用高德 API 自动补全
    if lng == 0 and lat == 0:
        lng, lat = geocode(body.get('province', ''), body.get('city', ''), body.get('district', ''), body.get('town', ''))
    ws_r.cell(new_region_row, R_LNG).value = lng
    ws_r.cell(new_region_row, R_LAT).value = lat
```

- [ ] **Step 2: 集成到 PUT /api/fruits (update_fruit)**

在 `update_fruit()` 函数中，更新产区坐标部分（约 380-389 行），在更新 location 字段时也补全坐标。

找到：
```python
    # If location changed, update matching region mapping
    ws_r = wb['产区映射表']
    for r in range(2, ws_r.max_row + 1):
        fids = str(ws_r.cell(r, R_FIDS).value or '').strip()
        if fruit_id in fids.split(','):
            if 'province' in current: ws_r.cell(r, R_PROV).value = current.get('province', '')
            if 'city' in current: ws_r.cell(r, R_CITY).value = current.get('city', '')
            if 'district' in current: ws_r.cell(r, R_DIST).value = current.get('district', '')
            if 'town' in current: ws_r.cell(r, R_TOWN).value = current.get('town', '')
            break
```

替换为：
```python
    # If location changed, update matching region mapping
    ws_r = wb['产区映射表']
    for r in range(2, ws_r.max_row + 1):
        fids = str(ws_r.cell(r, R_FIDS).value or '').strip()
        if fruit_id in fids.split(','):
            if 'province' in current: ws_r.cell(r, R_PROV).value = current.get('province', '')
            if 'city' in current: ws_r.cell(r, R_CITY).value = current.get('city', '')
            if 'district' in current: ws_r.cell(r, R_DIST).value = current.get('district', '')
            if 'town' in current: ws_r.cell(r, R_TOWN).value = current.get('town', '')
            # 若产区坐标为 0，调用高德 API 补全
            try: exist_lng = float(ws_r.cell(r, R_LNG).value or 0)
            except: exist_lng = 0
            try: exist_lat = float(ws_r.cell(r, R_LAT).value or 0)
            except: exist_lat = 0
            if exist_lng == 0 and exist_lat == 0:
                new_lng, new_lat = geocode(
                    current.get('province', ''), current.get('city', ''),
                    current.get('district', ''), current.get('town', ''))
                ws_r.cell(r, R_LNG).value = new_lng
                ws_r.cell(r, R_LAT).value = new_lat
            break
```

- [ ] **Step 3: 集成到 POST /api/sync-all**

在 `sync_all()` 的 regions 写入循环中（约 564-576 行），添加坐标补全。

找到：
```python
    for i, r in enumerate(regions_data):
        row_num = i + 2
        ws_r.cell(row_num, R_ADCODE).value = str(r.get('adcode', '')).strip()
        ws_r.cell(row_num, R_PROV).value = str(r.get('province', '')).strip()
        ws_r.cell(row_num, R_CITY).value = str(r.get('city', '')).strip()
        ws_r.cell(row_num, R_DIST).value = str(r.get('district', '')).strip()
        ws_r.cell(row_num, R_TOWN).value = str(r.get('town', '')).strip()
        try: ws_r.cell(row_num, R_LNG).value = float(r.get('lng', 0))
        except: ws_r.cell(row_num, R_LNG).value = 0
        try: ws_r.cell(row_num, R_LAT).value = float(r.get('lat', 0))
        except: ws_r.cell(row_num, R_LAT).value = 0
```

替换为：
```python
    for i, r in enumerate(regions_data):
        row_num = i + 2
        ws_r.cell(row_num, R_ADCODE).value = str(r.get('adcode', '')).strip()
        ws_r.cell(row_num, R_PROV).value = str(r.get('province', '')).strip()
        ws_r.cell(row_num, R_CITY).value = str(r.get('city', '')).strip()
        ws_r.cell(row_num, R_DIST).value = str(r.get('district', '')).strip()
        ws_r.cell(row_num, R_TOWN).value = str(r.get('town', '')).strip()
        try: lng = float(r.get('lng', 0))
        except: lng = 0
        try: lat = float(r.get('lat', 0))
        except: lat = 0
        # 若坐标为 0，调用高德 API 自动补全
        if lng == 0 and lat == 0:
            lng, lat = geocode(
                str(r.get('province', '')), str(r.get('city', '')),
                str(r.get('district', '')), str(r.get('town', '')))
        ws_r.cell(row_num, R_LNG).value = lng
        ws_r.cell(row_num, R_LAT).value = lat
```

- [ ] **Step 4: Commit**

```bash
git add server.py
git commit -m "feat: 创建/更新/同步产区时自动调用高德 API 补全坐标"
```

---

### Task 3: 前端 — 生长期 & 暂无数据合并为绿色空心圆

**Files:**
- Modify: `中国水果产区数据大屏.html` (phaseColors/phaseBorderColors/phaseNames/phasePriority, fruitPhase, buildScatterPoint, buildCityLabelPoints, tooltip formatter, legend HTML, phase-dot CSS)

- [ ] **Step 1: 修改 fruitPhase() — 删除 -1 返回路径**

找到 `fruitPhase()` 函数（约 366-375 行）。将函数体替换为：

```javascript
  function fruitPhase(fruit) {
    var ss = fruit.seasonStart, se = fruit.seasonEnd, ps = fruit.peakStart, pe = fruit.peakEnd, m = selectedMonth;
    if (!ss || !se || !ps || !pe) return 0;
    if (monthInRange(m, ps, pe)) return 2;
    if (ps > ss && m >= ss && m < ps) return 1;
    if (ps <= ss && (m >= ss || m < ps) && !monthInRange(m, pe === 12 ? 1 : pe+1, se)) return 1;
    if (monthInRange(m, pe === 12 ? 1 : pe+1, se)) return 3;
    return 0;
  }
```

关键改动：`if (!ss || !se || !ps || !pe) return 0;` (原为 `return -1`)，尾部的 `return 0;` (原为 `return -1`)。

- [ ] **Step 2: 修改 phase 映射表 — 删除 key '-1'，key 0 改为绿色空心**

找到（约 376-379 行）：
```javascript
  var phaseColors = {2:'#f04134', 3:'#7b4dff', 1:'#f5a623', 0:'#52c41a', '-1':'transparent'};
  var phaseBorderColors = {2:'#f04134', 3:'#7b4dff', 1:'#f5a623', 0:'#52c41a', '-1':'#bbb'};
  var phaseNames = {2:'盛产期', 3:'尾产期', 1:'成熟期', 0:'生长期', '-1':'暂无数据'};
  var phasePriority = {2:4, 3:3, 1:2, 0:1, '-1':0};
```

替换为：
```javascript
  var phaseColors = {2:'#f04134', 3:'#7b4dff', 1:'#f5a623', 0:'transparent'};
  var phaseBorderColors = {2:'#f04134', 3:'#7b4dff', 1:'#f5a623', 0:'#52c41a'};
  var phaseNames = {2:'盛产期', 3:'尾产期', 1:'成熟期', 0:'生长期'};
  var phasePriority = {2:4, 3:3, 1:2, 0:1};
```

- [ ] **Step 3: 修改 buildScatterPoint() — phase 0 为绿色空心圆**

找到 `buildScatterPoint()`（约 484-499 行）：
```javascript
  function buildScatterPoint(r) {
    var ph = regionPhase(r), color = phaseColors[ph], borderColor = phaseBorderColors[ph], isHollow = ph === -1;
    return {
      name: r.district || r.city,
      value: [r.lng, r.lat],
      province: r.province, city: r.city, district: r.district, town: r.town,
      level: r.level, fruitIds: r.fruitIds, adcode: r.adcode, phase: ph,
      symbolSize: isHollow ? 6 : (r.level === '核心产区' ? 10 : 7),
      itemStyle: {
        color: isHollow ? 'transparent' : color,
        borderColor: borderColor, borderWidth: isHollow ? 2 : 1.5,
        borderType: isHollow ? 'dashed' : 'solid',
        shadowBlur: isHollow ? 0 : 4, shadowColor: isHollow ? 'transparent' : color
      }
    };
  }
```

替换为：
```javascript
  function buildScatterPoint(r) {
    var ph = regionPhase(r), color = phaseColors[ph], borderColor = phaseBorderColors[ph], isHollow = ph === 0;
    return {
      name: r.district || r.city,
      value: [r.lng, r.lat],
      province: r.province, city: r.city, district: r.district, town: r.town,
      level: r.level, fruitIds: r.fruitIds, adcode: r.adcode, phase: ph,
      symbolSize: isHollow ? 6 : (r.level === '核心产区' ? 10 : 7),
      itemStyle: {
        color: color,
        borderColor: borderColor, borderWidth: isHollow ? 2 : 1.5,
        borderType: 'solid',
        shadowBlur: isHollow ? 0 : 4, shadowColor: isHollow ? 'transparent' : color
      }
    };
  }
```

关键改动：`isHollow = ph === 0` (原为 `ph === -1`)，color 直接用 `color` 而不再判 hollow，`borderType` 固定为 `'solid'`。

- [ ] **Step 4: 修改 buildCityLabelPoints() — 适配 phase 0**

找到 `buildCityLabelPoints()`（约 501-521 行），其中：
```javascript
      var bestReg = g.regs.find(function(r) { return regionPhase(r) >= 0; }) || g.regs[0];
```
保持不变 — 逻辑正确（phase 0 是正数，仍然 matched）。

下方 `itemStyle` 中：
```javascript
        itemStyle: { color: phaseColors[ph] || '#bbb', borderColor: '#fff', borderWidth: 2, shadowBlur: 6, shadowColor: 'rgba(0,0,0,0.15)' }
```
替换为：
```javascript
        itemStyle: { color: ph === 0 ? '#52c41a' : phaseColors[ph], borderColor: '#fff', borderWidth: 2, shadowBlur: 6, shadowColor: 'rgba(0,0,0,0.15)' }
```

- [ ] **Step 5: 修改 tooltip formatter — 删除 '暂无数据' 引用**

在 `_doRender()` 的 tooltip formatter 中（约 594-608 行），`phaseColors[ph] || '#bbb'` 和 `phaseNames[ph] || '暂无数据'` 的 fallback 改为生长期。实际上由于不会再有 `-1` phase，这些 fallback 不会触发，但安全起见保留不变也没关系。此处跳过修改。

- [ ] **Step 6: 更新图例 HTML — 5 项变 4 项**

找到（约 203-209 行）：
```html
    <div class="phase-legend">
      <span><span class="phase-dot green"></span> 生长期</span>
      <span><span class="phase-dot yellow"></span> 成熟期</span>
      <span><span class="phase-dot red"></span> 盛产期</span>
      <span><span class="phase-dot purple"></span> 尾产期</span>
      <span><span class="phase-dot hollow"></span> 暂无数据</span>
    </div>
```

替换为：
```html
    <div class="phase-legend">
      <span><span class="phase-dot green-hollow"></span> 生长期</span>
      <span><span class="phase-dot yellow"></span> 成熟期</span>
      <span><span class="phase-dot red"></span> 盛产期</span>
      <span><span class="phase-dot purple"></span> 尾产期</span>
    </div>
```

- [ ] **Step 7: 更新 CSS — phase-dot 新增 green-hollow 样式**

找到 CSS 中的 phase-dot 定义（约 77-82 行）：
```css
  .phase-dot.green { background: #52c41a; }
  .phase-dot.yellow { background: #f5a623; }
  .phase-dot.red { background: #f04134; }
  .phase-dot.purple { background: #7b4dff; }
  .phase-dot.hollow { background: transparent; border: 2px solid #bbb; width: 7px; height: 7px; }
```

替换为：
```css
  .phase-dot.green-hollow { background: transparent; border: 2px solid #52c41a; width: 7px; height: 7px; }
  .phase-dot.yellow { background: #f5a623; }
  .phase-dot.red { background: #f04134; }
  .phase-dot.purple { background: #7b4dff; }
```

同时更新 `.phase-dot` 基类（约 77 行），确保 box-sizing 一致：
```css
  .phase-dot { width: 9px; height: 9px; border-radius: 50%; box-sizing: border-box; }
```

- [ ] **Step 8: Server 验证 — 重启并生成新 HTML**

```bash
cd c:\fruit-ops && python -c "from server import save_and_regenerate; d=save_and_regenerate(); print('OK:', len(d['fruits']), 'fruits,', len(d['regions']), 'regions,', len(d['curves']), 'curves')"
```

- [ ] **Step 9: Commit**

```bash
git add 中国水果产区数据大屏.html
git commit -m "feat: 生长期与暂无数据合并为绿色空心圆"
```

---

### Task 4: 前端 — 地图交互重写（省份点击 zoom，不切子地图）

**Files:**
- Modify: `中国水果产区数据大屏.html` (renderMap, click handler, _doRender, drillState/logic, 删除 geoMapCache/loadGeoMap/getProvAdcode/getCityAdcode 等无用代码, 新增"回到全国"按钮逻辑)

- [ ] **Step 1: 删除不需要的子地图基础设施**

删除以下变量和函数（约 398-429 行）：

```javascript
  var geoMapCache = {};  // adcode -> mapName (删除)
  var provAdcodeMap = {}; // province short name -> adcode (删除，但保留在 admin 区的那个)
```

删除以下函数：
- `getProvAdcode()` (约 402-405 行)
- `getCityAdcode()` (约 408-416 行)
- `loadGeoMap()` (约 419-429 行)
- `getCityData()` (约 432-438 行)
- `getDistrictData()` (约 441-454 行)
- `matchMapFeature()` (约 524-531 行) — 保留但需修改（见 Step 3）

- [ ] **Step 2: 新增 zoom 状态追踪变量**

在 `drillState` 声明之后（约 397 行后）添加：

```javascript
  var zoomState = { center: [105, 36], zoom: 1.2, provName: null }; // 追踪当前 zoom 位置
```

- [ ] **Step 3: 重写 renderMap() — 始终使用中国地图，只改变 center/zoom**

将 `renderMap()` 函数（约 684-731 行）替换为：

```javascript
  function renderMap(optCenter, optZoom) {
    var dom = document.getElementById('chart-map');
    if (!dom || !mapChart) return;

    // 始终使用中国地图，所有散点可见
    var pdata = getProvData();
    var mapData = pdata.map(function(d) { return { name: d.name, full: d.full, value: d.value, core: d.core }; });

    var center = optCenter || zoomState.center;
    var zoom = optZoom != null ? optZoom : zoomState.zoom;

    _doRender('china', mapData, validRegions, center, zoom, false);
  }
```

- [ ] **Step 4: 重写 click handler — 省份 zoom、散点 zoom、回到全国**

替换 `_doRender()` 中的 click handler（约 621-681 行）为：

```javascript
    // 绑定点击事件
    mapChart.off('click');
    mapChart.on('click', function(params) {
      // 点击省份图斑 → zoom 到该省
      if (params.componentType === 'geo') {
        var pd = null;
        // 匹配点击的省份名
        var mapData = params.data;
        // 从 getProvData 结果中查找完整省名
        var pdata = getProvData();
        for (var i = 0; i < pdata.length; i++) {
          if (pdata[i].name === params.name || params.name.indexOf(pdata[i].name) === 0 || pdata[i].name.indexOf(params.name) === 0) {
            pd = pdata[i];
            break;
          }
        }
        if (pd && pd.full && provIndex[pd.full]) {
          var bnd = getProvBounds(pd.full);
          if (bnd) {
            var span = Math.max(bnd.lngSpan, bnd.latSpan, 0.5);
            var newZoom = Math.min(600 / span, 20);
            zoomState.center = bnd.center;
            zoomState.zoom = newZoom;
            zoomState.provName = pd.full;
            mapChart.setOption({
              geo: { center: bnd.center, zoom: newZoom }
            }, { notMerge: false });
            updateBackBtn();
            showProvinceDetail(pd.full);
          }
        }
        return;
      }

      // 点击散点 → 进一步 zoom 到该市区域
      if (params.seriesType === 'effectScatter' || params.seriesType === 'scatter') {
        if (!params.data.adcode) return;
        var r = regionMap[params.data.adcode];
        if (!r) return;
        if (!zoomState.provName) return; // 未 zoom 到省份前，点散点不做 zoom

        var cityName = r.city || r.province;
        var cityRegs = ((provIndex[zoomState.provName] || {}).cities || {})[cityName] || [];
        var bnd = boundsOf(cityRegs);
        if (bnd) {
          var span = Math.max(bnd.lngSpan, bnd.latSpan, 0.3);
          var newZoom = Math.min(800 / span, 25);
          zoomState.center = bnd.center;
          zoomState.zoom = newZoom;
          mapChart.setOption({
            geo: { center: bnd.center, zoom: newZoom }
          }, { notMerge: false });
          showRegionDetail(r);
        }
        return;
      }
    });
```

- [ ] **Step 5: 更新 updateBackBtn() — 基于 zoomState 显示/隐藏**

替换 `updateBackBtn()`（约 763-774 行）为：

```javascript
  function updateBackBtn() {
    var btn = document.getElementById('back-btn'), ds = zoomState;
    if (ds.provName) {
      btn.classList.add('show');
      btn.textContent = '回到全国';
    } else {
      btn.classList.remove('show');
    }
  }
```

- [ ] **Step 6: 更新 drillUp() — 恢复全国视图**

替换 `drillUp()`（约 733-746 行）为：

```javascript
  function drillUp() {
    zoomState = { center: [105, 36], zoom: 1.2, provName: null };
    mapChart.setOption({
      geo: { center: [105, 36], zoom: 1.2 }
    }, { notMerge: false });
    updateBackBtn();
    showEmptyDetail();
  }
  window.drillUp = drillUp;
```

- [ ] **Step 7: 清理 drillState 引用**

将代码中所有 `drillState` 引用替换为直接使用 level:
- `_doRender()` 中的 `var ds = drillState;` → 删除这行，将 `ds.level` 替换为固定值 `'nation'`（因为我们始终用中国地图）
- 简化 `_doRender()` 中的 scatter label 逻辑：`label: { show: false }`（不显示散点标签，因为始终是 nation view）
- 删除 `highlightRegions` 逻辑（用于高亮选中城市区域，不再需要）

具体：在 `_doRender()` 中（约 534-618 行）：

删除：
```javascript
    var ds = drillState;
```
并将后面用到 `ds.level` 的地方改为：
- `if (ds.level !== 'nation')` → 删除这行（符号大小统一）
- `label: { show: ds.level === 'city', ...}` → `label: { show: false, ...}`
- `symbolSize: ds.level === 'nation' ? 8 : 10` → 统一为 `symbolSize: 8`（核心产区）、`symbolSize: 5`（普通产区）
- `highlightRegions` 数组 → 清空为 `[]`

删掉 `showSubLabels` 参数和相关逻辑（城市标注点不再需要）：
- 函数签名中的 `showSubLabels` 参数
- `buildCityLabelPoints` 调用块（约 559-568 行）
- `if (ds.level === 'province')` 选中的高亮区域块（约 546-551 行）

- [ ] **Step 8: 删除 showDistrictDetailInPanel()**

该函数约 749-761 行，内容可以删除（不再有城市级别的地图）。但函数的引用 `showDistrictDetailInPanel` 已在 click handler 中删除，所以只需删除函数定义即可。

- [ ] **Step 9: Server 验证 — 重启并确认 HTML 可正常加载**

```bash
cd c:\fruit-ops && taskkill /F /IM python.exe 2>/dev/null; sleep 2; start /MIN python server.py && sleep 3 && python -c "import urllib.request; print(urllib.request.urlopen('http://localhost:5000/api/health').read().decode())"
```

- [ ] **Step 10: Commit**

```bash
git add 中国水果产区数据大屏.html
git commit -m "feat: 地图交互重写 — 省份点击平滑 zoom，移除子地图切换"
```

---

### Task 5: 端到端验证

- [ ] **Step 1: 启动服务器并验证**

```bash
cd c:\fruit-ops && start /MIN python server.py
```
等待 3 秒后，浏览器打开 `http://localhost:5000/`。

- [ ] **验证清单**：
  1. 点击省份（如"四川省"）→ 地图平滑 zoom 到四川省区域
  2. 点击散点 → 进一步 zoom 到该市区域
  3. "回到全国"按钮可见 → 点击恢复全国视图
  4. 生长期产区显示为绿色空心圆（绿边框+透明填充）
  5. 底部图例为 4 项（无"暂无数据"）
  6. 新增产区时坐标自动从高德 API 获取

- [ ] **Step 2: 修复发现的问题并 commit**

---

## 风险提示

1. **高德 API 日限额**：免费版 5000 次/日，同步大量数据时可能超限。`sync-all` 中对每个 `lng=0` 的项都调用 API，建议初次同步后大部分坐标已补全，后续仅增量调用。
2. **高德 API 签名**：`sig` 参数使用 MD5 签名，如高德升级到 HMAC-SHA256 验证需相应调整。
3. **地图缩放边界**：`scaleLimit.max = 25`，城市 zoom 计算中 `Math.min(800/span, 25)` 已限制最大值为 25。
